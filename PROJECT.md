# PROJECT.md — دستور مشروع "محاكي أماديوس"

> **ملحوظة مهمة:** الملف ده هو المرجع الثابت لأي جلسة جديدة (مع Claude أو مع Kimi أو أي مساعد تاني).  
> أي حد هيكمل الشغل على المشروع، أول حاجة يعملها إنه يقرأ الملف ده كامل قبل ما يقترح أي تعديل أو يبدأ يكتب كود.

---

## 1. الهدف من المشروع
بناء محاكي نصي احترافي لنظام **Amadeus GDS** (نظام حجوزات الطيران).  
الهدف مش إن المحاكي "يشتغل" بس، الهدف إن اللي يتدرب عليه لما يقعد على النظام الحقيقي في الشغل، يحس إنه شايف الحاجة دي قبل كده. يعني كل تفصيلة — شكل الشاشة، طريقة كتابة الأوامر، رسائل الأخطاء، حتى الاختصارات الغريبة — لازم تكون قريبة من الواقع قد ما نقدر، مش نسخة مبسطة أو "تمثيل" للفكرة.  
**المقياس اللي هنحكم بيه على كل مرحلة:** لو حد اتدرب على المحاكي وبعدين قعد على أماديوس الحقيقي، هل هيحس إنه غريب عليه ولا مألوف؟

---

## 2. الجمهور المستهدف
* مستخدم واحد بس، مبتدئ تمامًا في أماديوس.
* بيتعلم عشان يشتغل موظف سفريات (Travel Agent).
* من غير خبرة برمجة سابقة — يعني أي تعليمات لازم تكون خطوة بخطوة وواضحة.
* بيبني المشروع كله من الموبايل، مفيش لابتوب أو بيئة تطوير تقليدية.
* **هذا يعني:** أي شرح تقني لازم يترجم لخطوات عملية بسيطة (افتح الملف الفلاني، انسخ الكود ده، حطه هنا)، مش افتراض إن المستخدم عارف يشغّل بيئة تطوير أو يستخدم Terminal.

---

## 3. حزمة التقنيات (Tech Stack) — نهائية، من غير تغيير

| الجزء | التقنية |
| :--- | :--- |
| **الواجهة** | HTML + CSS + JavaScript خام (Vanilla) — من غير أي Framework زي React أو Vue |
| **البيانات المرجعية** | ملفات JSON ثابتة (مطارات، شركات طيران، RBD، حالات PNR، رسائل أخطاء...) |
| **تخزين تقدم المستخدم** | localStorage في متصفح الموبايل |
| **الاستضافة** | GitHub Pages — شغالة بالفعل على `karimjan0096656-ops.github.io/amadeus-simulator` |
| **Backend** | مفيش خالص — كل حاجة بتشتغل جوه المتصفح |

*أي اقتراح مستقبلي (حتى لو مفيد تقنيًا) يخالف الجدول ده، لازم يتناقش الأول قبل ما يتنفذ.*

---

## 4. هيكلة الملفات والمجلدات

```text
amadeus-simulator/
├── index.html                 # الصفحة الرئيسية (شاشة الطرفية)
├── PROJECT.md                 # هذا الملف
│
├── css/
│   └── terminal.css           # شكل الطرفية (الألوان، الخطوط، تحاكي شاشة أماديوس الحقيقية)
│
├── js/
│   ├── main.js                # نقطة البداية، ربط كل الأجزاء ببعض
│   ├── parser.js               # محرك تحليل الأوامر (Command Parser)
│   ├── pnr.js                  # محرك بناء وإدارة الـ PNR
│   ├── leveltest.js            # اختبار تحديد المستوى (المرحلة 3)
│   ├── errors.js               # محرك تصحيح الأخطاء الذكي
│   ├── pricing.js              # محرك الأسعار والرسوم والأمتعة (المرحلة 6)
│   ├── queues.js               # إدارة الطوابير (QT/QC)
│   ├── coaching.js             # منطق الإرشاد التدريجي وشخصيات العملاء
│   └── storage.js              # طبقة التعامل مع localStorage
│
├── data/
│   ├── airports.json           # قاعدة بيانات المطارات
│   ├── airlines.json           # قاعدة بيانات شركات الطيران
│   ├── rbd.json                # فئات الحجز (Booking Classes)
│   ├── flights.json            # بيانات الرحلات (بذرة المرحلة 1)
│   ├── level-test.json         # مهام اختبار تحديد المستوى (المرحلة 3)
│   ├── pnr-status.json         # أكواد حالة الـ PNR (Action Codes)
│   ├── errors.json             # كتالوج رسائل الأخطاء
│   ├── fares.json               # بيانات الأسعار والضرائب (المرحلة 6)
│   ├── ssr.json                 # الخدمات الخاصة (SSR) والفنادق والسيارات
│   └── scenarios.json           # شخصيات العملاء وسيناريوهات التدريب
│
└── assets/                     # أي خطوط أو أيقونات لو احتجناها
```

*كل ملف JSON بيتوسع تدريجيًا حسب المرحلة (نبدأ بيانات مصغّرة في المرحلة 1، ونوسّعها كاملة في المرحلة 4).*

---

## 5. خطة المراحل (12 مرحلة)

*كل مرحلة لازم تتقفل واختبارها ينجح قبل ما نبدأ اللي بعدها. مفيش قفز لمرحلة تالية وسط مرحلة ناقصة.*

### المرحلة 0 — التجهيز ✅ (تم)
* عمل الحسابات اللازمة (GitHub).
* إنشاء المستودع (Repository) وتفعيل GitHub Pages.
* كتابة هذا الملف (PROJECT.md).

### المرحلة 1 — بيانات أولية مصغّرة ✅ (تم)
* حوالي 20 مطار وشركة طيران كبرى.
* أساسيات فئات الحجز (RBD).

### المرحلة 2 — المحرك الأساسي ✅ (تم)
* Parser لأمر واحد بسيط (مثلًا أمر توفر الرحلات).
* بناء PNR بسيط لحد أمر الإنهاء (RF).

### المرحلة 3 — اختبار تحديد المستوى ✅ (تم)
* فوق نفس محرك المرحلة 2 (من غير تعديل في `parser.js` أو `pnr.js`).
* أمر داخلي `LEVELTEST`، 5 مهام بحد أقصى محاولتين لكل مهمة، تقرير نهائي بالمستوى (مبتدئ/متوسط/متقدم).
* النتيجة في الذاكرة بس (مفيش `storage.js` لسه) — التخزين الدائم هيتضاف مع محرك التقدم لاحقًا.

### المرحلة 4 — توسعة قاعدة البيانات الكاملة ✅ (تم)
* `data/airports.json`: اتوسّع لـ 205 مطار (تغطية كاملة للشرق الأوسط ومصر والخليج + كل المحاور الكبرى عالميًا).
* `data/airlines.json`: اتوسّع لـ 113 شركة طيران.
* `data/flights.json`: اتوسّع لـ 60 رحلة (سيناريو CAI-DXB 15JUL بتاع LEVELTEST اتحافظ عليه حرفيًا وبنفس الترتيب).
* `data/pnr-status.json` و `data/errors.json`: ملفين جداد، مرجعيين لحد المرحلة 5.
* من هنا فصاعدًا المحاكي بيبقى شامل مش عينة.

### المرحلة 5 — محرك تصحيح الأخطاء الذكي ✅ (تم)
* 4 خطوات ثابتة لتصحيح الخطأ.
* تصنيف الأخطاء (خطأ صياغة، خطأ منطقي، خطأ بيانات... إلخ).
* الهدف: المستخدم يتعلم من غلطه فورًا بدل ما يتوه.

### المرحلة 6 — الأسعار والرسوم ✅ (تم)
* Fare Display (أمر FQD — بيعرض كل فئات الحجز الـ 10 على مسار معين، مرتبة من الأرخص للأغلى — Best Buy).
* تسعير الحجز الحالي (أمر FXP — بيسعّر آخر Segment محجوز في الـ PNR بفئته بالظبط، مش أي فئة تانية).
* الأمتعة والضرائب مبيّنين مع كل فئة سعر.
* محرك حساب منفصل: `js/pricing.js` (نفس فلسفة فصل المسؤوليات بتاعة `pnr.js` — `parser.js` بيتحقق من الصياغة وكود المطارات ويستدعي، و`pricing.js` بيحسب ويرجّع بيانات جاهزة، مش نصوص معروضة).
* نطاق تغطية `data/fares.json` الحالي: 3 مسارات بس لحد دلوقتي — CAI-DXB، DXB-LHR، CAI-JED (مغطاة بجميع فئات الحجز الـ 10 الكاملة: F, A, J, C, D, Y, B, M, H, K).
* ⚠ **ملحظة مهمة:** احتاج الأمر تعديل صغير وضروري في `main.js` (تحميل `data/fares.json` ونداء `initPricing`) لضمان تحضير المحرك بالبيانات بشكل صحيح.

### المرحلة 7 — الخدمات الإضافية ✅ (تم)
* الفنادق والسيارات.
* Timatic مبسّط (متطلبات الدخول والفيزا).
* SSR (الخدمات الخاصة زي الأكل الخاص، الكرسي المتحرك... إلخ).

### المرحلة 8 — إدارة الطوابير ✅ (تم)
* أوامر QT / QC وما يتعلق بيها (`js/queues.js`).

### المرحلة 9 — الإرشاد التدريجي + شخصيات العملاء ✅ (تم)
* ملفات جديدة: `js/coaching.js` (منطق السيناريوهات والتلميحات)، `data/scenarios.json` (6 سيناريوهات عملاء).
* أوامر/كلمات جديدة: `SCENARIOS` (عرض القائمة)، `SCENARIO<رقم>` (بدء سيناريو)، `SCENARIOEXIT` (خروج)، `COACHINGON`/`COACHINGOFF` (تفعيل/تعطيل التلميحات).

#### ملاحظات وتصحيحات أثناء تنفيذ المرحلة 9:
1. `js/storage.js` المذكور في الهيكلة الأصلية لسه ما اتعملش فعليًا لحد المرحلة دي — عشان كده الـ Coaching اتبنى session-only (حالة السيناريو النشط وعداد الأخطاء بيتصفروا لو الصفحة اتعمل لها Refresh).
2. الـ Coaching بيراقب نتيجة الأوامر بعد ما `parseCommand()` يرجّع الرد الحقيقي — مفيش أي لمس لـ `parser.js` أو `pnr.js` أو `errors.js`. التلميحات بتتحط في سطر منفصل (`coaching-line`) في `main.js`.
3. التلميح بيترقّى من عام لأدق (specific) بعد ثاني غلطة في نفس الخطوة، وبيتحط تنبيه إضافي لو فيه وقفة أطول من 45 ثانية بين الأوامر.
4. القيود الأساسية (راكب واحد، Segment واحد) اتحترمت في تصميم الـ 6 سيناريوهات كلها — أقصى تعقيد "عميل واحد بيحجز لمناسبة/طلب خاص".
5. اختبار انحدار كامل (AN/SS/NM/AP/TK/RF/ER) اتعمل ونجح بالكامل.

### المرحلة 10 — التكيف مع الأداء + سيناريوهات الضغط
* المحاكي يتكيف مع مستوى أداء المستخدم.
* سيناريوهات ضغط تحاكي ظروف الشغل الحقيقي (عميل مستعجل، مشكلة في الحجز وقت الذروة... إلخ).

### المرحلة 11 — بوابة مراجعة
* محاذاة كل اللي اتبنى مع مسار شهادة Amadeus Basic/Advanced الرسمي.
* مراجعة شاملة نتأكد فيها إن مفيش فجوة بين المحاكي والامتحان الحقيقي.

### المرحلة 12 — الاختبار النهائي
* محاكاة وردية شغل كاملة.
* Free Mode (المستخدم يستخدم كل اللي اتعلمه من غير مساعدة أو إرشاد).

---

## 6. مبادئ عمل ثابتة
1. **الدقة قبل السرعة:** أي مرحلة ما تتقفلش إلا لما تشتغل صح فعلًا، مش "شكلها شغالة".
2. **الواقعية أولوية:** أي اختيار بين "أسهل في البناء" و"أقرب للنظام الحقيقي" — نختار الأقرب للحقيقي.
3. **كل حاجة Client-Side:** ولا خطوة تتطلب سيرفر أو حساب خارجي غير المذكور فوق.
4. **خطوة بخطوة على الموبايل:** أي تعليمات بناء لازم تكون قابلة للتنفيذ من موبايل بدون خبرة برمجة.
5. **الملف ده هو المرجع:** أي تعديل في الهدف أو حزمة التقنيات أو خطة المراحل، لازم يتسجل هنا أول ما يتفق عليه، مش يفضل في ذاكرة المحادثة بس.

---

## 7. روابط مهمة
* **المستودع والموقع المباشر:** [amadeus-simulator](https://karimjan0096656-ops.github.io/amadeus-simulator)

---

**آخر تحديث:** المرحلة 9 — الإرشاد التدريجي وشخصيات العملاء (`SCENARIOS` / `coaching.js`).
