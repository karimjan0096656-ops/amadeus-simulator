PROJECT.md — دستور مشروع "محاكي أماديوس"
ملحوظة مهمة: الملف ده هو المرجع الثابت لأي جلسة جديدة (مع Claude أو مع Kimi أو أي مساعد تاني).
أي حد هيكمل الشغل على المشروع، أول حاجة يعملها إنه يقرأ الملف ده كامل قبل ما يقترح أي تعديل أو يبدأ يكتب كود.
1. الهدف من المشروع
بناء محاكي نصي احترافي لنظام Amadeus GDS (نظام حجوزات الطيران).
الهدف مش إن المحاكي "يشتغل" بس، الهدف إن اللي يتدرب عليه لما يقعد على النظام الحقيقي في الشغل، يحس إنه شايف الحاجة دي قبل كده. يعني كل تفصيلة — شكل الشاشة، طريقة كتابة الأوامر، رسائل الأخطاء، حتى الاختصارات الغريبة — لازم تكون قريبة من الواقع قد ما نقدر، مش نسخة مبسطة أو "تمثيل" للفكرة.
المقياس اللي هنحكم بيه على كل مرحلة: لو حد اتدرب على المحاكي وبعدين قعد على أماديوس الحقيقي، هل هيحس إنه غريب عليه ولا مألوف؟
2. الجمهور المستهدف
مستخدم واحد بس، مبتدئ تمامًا في أماديوس.
بيتعلم عشان يشتغل موظف سفريات (Travel Agent).
من غير خبرة برمجة سابقة — يعني أي تعليمات لازم تكون خطوة بخطوة وواضحة.
بيبني المشروع كله من الموبايل، مفيش لابتوب أو بيئة تطوير تقليدية.
هذا يعني: أي شرح تقني لازم يترجم لخطوات عملية بسيطة (افتح الملف الفلاني، انسخ الكود ده، حطه هنا)، مش افتراض إن المستخدم عارف يشغّل بيئة تطوير أو يستخدم Terminal.
3. حزمة التقنيات (Tech Stack) — نهائية، من غير تغيير
الجزء
التقنية
الواجهة
HTML + CSS + JavaScript خام (Vanilla) — من غير أي Framework زي React أو Vue
البيانات المرجعية
ملفات JSON ثابتة (مطارات، شركات طيران، RBD، حالات PNR، رسائل أخطاء...)
تخزين تقدم المستخدم
localStorage في متصفح الموبايل
الاستضافة
GitHub Pages — شغالة بالفعل على karimjan0096656-ops.github.io/amadeus-simulator
Backend
مفيش خالص — كل حاجة بتشتغل جوه المتصفح
أي اقتراح مستقبلي (حتى لو مفيد تقنيًا) يخالف الجدول ده، لازم يتناقش الأول قبل ما يتنفذ.
4. هيكلة الملفات والمجلدات
amadeus-simulator/
├── index.html                 # الصفحة الرئيسية (شاشة الطرفية)
├── PROJECT.md                 # هذا الملف
│
├── css/
│   └── terminal.css           # شكل الطرفية (الألوان، الخطوط، تحاكي شاشة أماديوس الحقيقية)
│
├── js/
│   ├── main.js                # نقطة البداية، ربط كل الأجزاء ببعض
│   ├── parser.js               # محرك تحليل الأوامر (Command Parser)
│   ├── pnr.js                  # محرك بناء وإدارة الـ PNR
│   ├── leveltest.js            # اختبار تحديد المستوى (المرحلة 3)
│   ├── errors.js               # محرك تصحيح الأخطاء الذكي
│   ├── pricing.js              # محرك الأسعار والرسوم والأمتعة (المرحلة 6)
│   ├── ancillary.js            # بيانات ومنطق الفنادق/السيارات/SSR/Timatic (المرحلة 7)
│   ├── queues.js               # إدارة الطوابير (QT/QC/QS/QN/QI/QE) (المرحلة 8)
│   ├── coaching.js             # منطق الإرشاد التدريجي وشخصيات العملاء (المرحلة 9)
│   ├── storage.js              # طبقة التعامل مع localStorage (المرحلة 10)
│   ├── performance.js          # قياس إحصائيات الأداء والتكيف (المرحلة 10)
│   └── seatmaps.js             # خرائط المقاعد واختيار مقعد (المرحلة 11 — SM/ST)
│
├── data/
│   ├── airports.json           # قاعدة بيانات المطارات
│   ├── airlines.json           # قاعدة بيانات شركات الطيران
│   ├── rbd.json                # فئات الحجز (Booking Classes)
│   ├── flights.json            # بيانات الرحلات (بذرة المرحلة 1)
│   ├── level-test.json         # مهام اختبار تحديد المستوى (المرحلة 3)
│   ├── pnr-status.json         # أكواد حالة الـ PNR (Action Codes)
│   ├── errors.json             # كتالوج رسائل الأخطاء
│   ├── fares.json               # بيانات الأسعار والضرائب (المرحلة 6)
│   ├── hotels.json              # بيانات الفنادق (المرحلة 7)
│   ├── cars.json                # بيانات تأجير السيارات (المرحلة 7)
│   ├── ssr.json                 # الخدمات الخاصة (SSR) (المرحلة 7)
│   ├── timatic.json             # بيانات Timatic المبسّطة (المرحلة 7)
│   ├── queues.json              # بيانات الطوابير المبدئية (المرحلة 8)
│   ├── scenarios.json           # شخصيات العملاء وسيناريوهات التدريب (المرحلة 9)
│   └── seatmaps.json            # بيانات خرائط المقاعد المبسّطة (المرحلة 11 — SM/ST)
│
└── assets/                     # أي خطوط أو أيقونات لو احتجناها
كل ملف JSON بيتوسع تدريجيًا حسب المرحلة (نبدأ بيانات مصغّرة في المرحلة 1، ونوسّعها كاملة في المرحلة 4).
5. خطة المراحل (12 مرحلة)
كل مرحلة لازم تتقفل واختبارها ينجح قبل ما نبدأ اللي بعدها. مفيش قفز لمرحلة تالية وسط مرحلة ناقصة.
المرحلة 0 — التجهيز ✅ (تم)
عمل الحسابات اللازمة (GitHub).
إنشاء المستودع (Repository) وتفعيل GitHub Pages.
كتابة هذا الملف (PROJECT.md).
المرحلة 1 — بيانات أولية مصغّرة ✅ (تم)
حوالي 20 مطار وشركة طيران كبرى.
أساسيات فئات الحجز (RBD).
المرحلة 2 — المحرك الأساسي ✅ (تم)
Parser لأمر واحد بسيط (مثلًا أمر توفر الرحلات).
بناء PNR بسيط لحد أمر الإنهاء (RF).
المرحلة 3 — اختبار تحديد المستوى ✅ (تم)
فوق نفس محرك المرحلة 2 (من غير تعديل في parser.js أو pnr.js).
أمر داخلي LEVELTEST، 5 مهام بحد أقصى محاولتين لكل مهمة، تقرير نهائي بالمستوى (مبتدئ/متوسط/متقدم).
النتيجة في الذاكرة بس (مفيش storage.js لسه) — التخزين الدائم هيتضاف مع محرك التقدم لاحقًا.
المرحلة 4 — توسعة قاعدة البيانات الكاملة ✅ (تم)
data/airports.json: اتوسّع لـ 205 مطار (تغطية كاملة للشرق الأوسط ومصر والخليج + كل المحاور الكبرى عالميًا).
data/airlines.json: اتوسّع لـ 113 شركة طيران.
data/flights.json: اتوسّع لـ 60 رحلة (سيناريو CAI-DXB 15JUL بتاع LEVELTEST اتحافظ عليه حرفيًا وبنفس الترتيب).
data/pnr-status.json و data/errors.json: ملفين جداد، مرجعيين لحد المرحلة 5.
من هنا فصاعدًا المحاكي بيبقى شامل مش عينة.
المرحلة 5 — محرك تصحيح الأخطاء الذكي ✅ (تم)
4 خطوات ثابتة لتصحيح الخطأ.
تصنيف الأخطاء (خطأ صياغة، خطأ منطقي، خطأ بيانات... إلخ).
الهدف: المستخدم يتعلم من غلطه فورًا بدل ما يتوه.
المرحلة 6 — الأسعار والرسوم ✅ (تم)
Fare Display (أمر FQD — بيعرض كل فئات الحجز الـ 10 على مسار معين، مرتبة من الأرخص للأغلى — Best Buy).
تسعير الحجز الحالي (أمر FXP — بيسعّر آخر Segment محجوز في الـ PNR بفئته بالظبط، مش أي فئة تانية).
الأمتعة والضرائب مبيّنين مع كل فئة سعر.
محرك حساب منفصل: js/pricing.js (نفس فلسفة فصل المسؤوليات بتاعة pnr.js — parser.js بيتحقق من الصياغة وكود المطارات ويستدعي، وpricing.js بيحسب ويرجّع بيانات جاهزة، مش نصوص معروضة).
نطاق تغطية data/fares.json الحالي: 3 مسارات بس لحد دلوقتي — CAI-DXB، DXB-LHR، CAI-JED (مغطاة بجميع فئات الحجز الـ 10 الكاملة: F, A, J, C, D, Y, B, M, H, K).
⚠ ملحظة مهمة: احتاج الأمر تعديل صغير وضروري في main.js (تحميل data/fares.json ونداء initPricing) لضمان تحضير المحرك بالبيانات بشكل صحيح.
المرحلة 7 — الخدمات الإضافية ✅ (تم)
الفنادق والسيارات.
Timatic مبسّط (متطلبات الدخول والفيزا).
SSR (الخدمات الخاصة زي الأكل الخاص، الكرسي المتحرك... إلخ).
المرحلة 8 — إدارة الطوابير ✅ (تم)
أوامر QT / QC وما يتعلق بيها (js/queues.js).
المرحلة 9 — الإرشاد التدريجي + شخصيات العملاء ✅ (تم)
ملفات جديدة: js/coaching.js (منطق السيناريوهات والتلميحات)، data/scenarios.json (6 سيناريوهات عملاء).
أوامر/كلمات جديدة: SCENARIOS (عرض القائمة)، SCENARIO<رقم> (بدء سيناريو)، SCENARIOEXIT (خروج)، COACHINGON/COACHINGOFF (تفعيل/تعطيل التلميحات).
ملاحظات وتصحيحات أثناء تنفيذ المرحلة 9:
js/storage.js المذكور في الهيكلة الأصلية لسه ما اتعملش فعليًا لحد المرحلة دي — عشان كده الـ Coaching اتبنى session-only (حالة السيناريو النشط وعداد الأخطاء بيتصفروا لو الصفحة اتعمل لها Refresh).
الـ Coaching بيراقب نتيجة الأوامر بعد ما parseCommand() يرجّع الرد الحقيقي — مفيش أي لمس لـ parser.js أو pnr.js أو errors.js. التلميحات بتتحط في سطر منفصل (coaching-line) في main.js.
التلميح بيترقّى من عام لأدق (specific) بعد ثاني غلطة في نفس الخطوة، وبيتحط تنبيه إضافي لو فيه وقفة أطول من 45 ثانية بين الأوامر.
القيود الأساسية (راكب واحد، Segment واحد) اتحترمت في تصميم الـ 6 سيناريوهات كلها — أقصى تعقيد "عميل واحد بيحجز لمناسبة/طلب خاص".
اختبار انحدار كامل (AN/SS/NM/AP/TK/RF/ER) اتعمل ونجح بالكامل.
المرحلة 10 — التكيف مع الأداء + سيناريوهات الضغط ✅ (تم)
ملفات جديدة: js/storage.js (طبقة تعامل مع localStorage، مع fallback آمن لو مش متاح)، js/performance.js (تجميع إحصائيات الأداء + حساب مستوى التكيف الحالي).
js/coaching.js اتعدّل (من غير أي لمس لـ parser.js/pnr.js/errors.js): كثافة التلميحة المحددة (🎯) بقت متغيرة حسب مستوى التكيف اللي بيرجعه getAdaptiveTier():
أداء قوي (reduced) → التلميحة المحددة بتتأخر للغلطة التالتة.
أداء عادي (normal) → زي المرحلة 9 بالظبط، الغلطة التانية.
أداء ضعيف متكرر (increased) → التلميحة المحددة بتظهر من أول غلطة على طول.
التكيف مبني على الدقة (نسبة صح-من-أول-مرة في آخر 10 خطوات) مش على السرعة، اتساقًا مع مبدأ "الدقة قبل السرعة" في قسم 6 — لو اعتمدنا على السرعة كان هيكافئ عكس المبدأ ده بالظبط.
اقتراح إعادة سيناريو تلقائي (🔁) بيظهر لما المتدرب يعمل 3 أخطاء أو أكتر في نفس السيناريو.
ملحوظة إضافية (📌) بتظهر لو نوع الخطأ ده نقطة ضعف متكررة عبر كل تاريخ التمرين (5 أخطاء أو أكتر تراكميًا)، مش بس الجلسة الحالية — ده امتداد لفكرة checkRecurringPattern في errors.js بس على مستوى أطول من الوقت.
data/scenarios.json: 3 سيناريوهات ضغط جديدة (7، 8، 9) — عميل بيتكلم كتير ومحتاج تصفية المعلومات المهمة، عميلة مستعجلة محتاجة مقارنة أكتر من سطر في شاشة التوفر قبل القرار، وعميل مصمم على كلاس معين ممكن يكون شكله متاح بس مش بيتقبل في البيع فعليًا.
قرار storage.js (كان مفتوح من نهاية المرحلة 9): اتقرر الخيار الأول — إحصائيات الأداء (عدد الأخطاء حسب النوع، عدد السيناريوهات المكتملة، تاريخ مختصر للسيناريوهات) بقت بتتحفظ بين الجلسات عبر localStorage. حالة السيناريو النشط نفسه (activeScenario, currentStepIndex...) لسه session-only زي المرحلة 9 بالظبط — الفرق الوحيد إن "الأداء العام عبر الوقت" بقى محفوظ، مش "أنا فين دلوقتي في السيناريو." مفيش أي بيانات حساسة أو شخصية بتتخزن — أرقام وتصنيفات فقط.
ملاحظات وتصحيحات أثناء تنفيذ المرحلة 10:
اتلقى Bug موجود من قبل (مش من المرحلة دي) في main.js: شرط isQueueModeActive() كان مكرر مرتين ورا بعض (سطر فاضي وبعده نفس الشرط بمحتواه الحقيقي) — على الأغلب حصل غلط لصق وقت تنفيذ المرحلة 8. اتصحح بحذف السطر الفاضي المكرر بس، من غير أي تغيير في سلوك Queue Mode الفعلي.
سيناريو 9 (الكلاس اللي شكله متاح بس مش بيتقبل بيع) اتأكد فعليًا ببيانات flights.json الحقيقية (اتراجعت لاحقًا بعد التسليم الأول) — رحلة MS971 الساعة 1430 (CAI-DXB) عندها C:0 وD:0 (درجة رجال أعمال مقفولة تمامًا) رغم إن J:4 مفتوحة على نفس الرحلة. السيناريو اتظبط بناءً على المثال الحقيقي ده تحديدًا (مش افتراض عام).
سيناريو 8 (أنسب رحلة في التوفر) اتأكد كمان من نفس البيانات: الرحلة الوحيدة اللي بتقلع الصبح بدري على مسار CAI-DXB يوم 15JUL هي MS959 الساعة 0820 — النصوص اتظبطت لتشاور على الوقت ده بالتحديد بدل وصف عام. النظام الحالي برضه بيتحقق من ترتيب أكواد الأوامر مش من أي سطر بالظبط اتختار في SS، فمفيش تصحيح آلي لو المتدرب اختار سطر غلط — القيمة التدريبية في عملية المقارنة نفسها.
اختبار انحدار كامل اتعمل بمحاكاة منفصلة (Node، خارج المشروع، مش جزء من التسليم) قبل التسليم وغطّى: سيناريو 1 القديم كامل صح (نفس سلوك المرحلة 9 بالظبط، مفيش تلميحة لخطوة صح من أول مرة)، سيناريو ضغط جديد (8) كامل صح، أداء ضعيف متكرر بيرفع التوجيه فعلًا (increased)، أداء قوي متكرر بيأخر التلميحة فعلًا (reduced)، اقتراح إعادة السيناريو، خروج مبكر (SCENARIOEXIT) من غير كراش، COACHINGOFF بيوقف العرض بس بيسجل الإحصائيات، والأهم: البيانات فضلت محفوظة بعد محاكاة Refresh فعلية.
المرحلة 11 — بوابة مراجعة (Certification Alignment Review) ✅ (تم)
هدف المرحلة: مراجعة وتدقيق مش بناء ميزة جديدة — مقارنة كل اللي اتبنى (المراحل 0-10) مع مسار شهادة Amadeus Basic/Advanced، سطر بسطر مش انطباع عام.
قرار المرجع (قسم 3 من سبك المرحلة): اتقرر الخيار الثاني — المقارنة اعتمدت على المعرفة العامة الصناعية المتاحة عن منهج Amadeus Basic/Advanced (مصادر تدريب عامة اتراجعت وقت التنفيذ)، مش على مواد كورس EgyptAir الرسمي بتاع Malik (متاحتش وقتها). عشان كده كل نتيجة المراجعة تقريبية، مش رسمية 100%.
الجرد اتبنى فعليًا من parser.js + pnr.js + main.js + coaching.js + queues.js + pricing.js + ancillary.js (اتراجعوا كاملين، مش من وصف PROJECT.md بس).
ملخص نتيجة المراجعة: من إجمالي 41 نقطة مقارنة حقيقية (بعد استبعاد 6 كلمات داخلية للمحاكي زي LEVELTEST/SCENARIOS مش من أوامر أماديوس أصلًا):
✅ 21 مغطى بالكامل — كل الأوامر الأساسية المُنفَّذة لحد المرحلة 10 (AN, SS, NM, AP, TK, RF, ER, FQD, FXP, HA, HS, CA, CS, SR, TI, QT, QC, QS, QN, QI, QE).
⚠ 5 استثناء مقصود — 4 منهم ناتجة عن قيد "راكب واحد وSegment واحد" الموثّق من أول المشروع (أسماء ركاب متعددين، Group Bookings، Split PNR، رحلات متعددة Segments)، والخامس (Open/Ghost/Arrival Unknown Segment) اتصنّف كذلك أثناء تنفيذ الدفعة 7 لنفس السبب (تفصيل في التقرير).
❌→✅ 16 فجوة حقيقية — كلها اتقفلت. 14 بأوامر/سلوك جديد فعلي (RT, XE, IG, ET, SN, APM, APE, TKTL, RM, OS, TTP, FXB, QD, DAC/DNA, FQN/FQR, SM/ST, Waitlist، Direct Sell)، و2 (جزء من بند واحد) اتصنّفوا ⚠ بدل ما يتسدّوا (السطر اللي فوق).
القرار النهائي (بتأكيد Malik): سد كل الـ16 فجوة فورًا، على 7 دفعات مُختبرة (Batch)، كل دفعة بتتضاف وتتأكد باختبار انحدار كامل قبل الانتقال للي بعدها، حفاظًا على مبدأ "الدقة أهم من السرعة" رغم إن القرار سد فوري. الدفعات 4-7 اتنفذت باعتماد قرارات تصميمية موثّقة بوضوح (مش افتراضات مخفية) لما البيانات الفعلية مكانتش متاحة — تفصيل كامل لكل قرار في certification-alignment-report.md.
الدفعة 1 (منجزة ومُختبرة): RT (استرجاع PNR بكوده)، XE (إلغاء عنصر من الحجز المفتوح)، IG (تجاهل بدون حفظ)، ET (إنهاء مختصر بدون شاشة استرجاع كاملة) — الأربعة كانوا أعلى أولوية في التقرير.
الدفعة 2 (منجزة ومُختبرة): SN (جدول رحلات)، APM/APE (تواصل موبايل/إيميل إضافي)، TKTL (ترتيب تذكرة مؤجل)، RM/OS (ملاحظة/OSI حرة).
الدفعة 3 (منجزة ومُختبرة): TTP (إصدار تذكرة فعلي)، FXB (تسعير أرخص فئة متاحة)، QD (حذف مباشر من طابور).
اختبار الانحدار التراكمي عبر الدفعات الثلاثة: 61 حالة، 61 نجحت، 0 فشل — شامل تأكيد متكرر إن الأوامر القديمة (AN→ER, FQD, QT) فضلت شغالة زي ما هي بالظبط بعد كل دفعة.
الدفعة 4: DAC/DNA (فك تشفير كود مطار/شركة طيران — بأمانة، مع رسالة صريحة لو حقل الاسم مش موجود في البيانات بدل اختلاقه).
الدفعة 5: FQN/FQR (قواعد وقيود السعر — بأمانة برضه، رسالة صريحة لو fares.json مفهوش حقل قواعد لفئة معينة).
الدفعة 6: SM/ST (خرائط مقاعد) — ملف بيانات جديد بالكامل data/seatmaps.json + منطق منفصل js/seatmaps.js. كشف اختبار الدفعة دي باج حقيقي قديم من المرحلة 2 (aircraftType مكانش بينتسخ للـSegment المحجوز) — اتصحح.
الدفعة 7 (الأحساس، بتلمس handleSS): دعم Waitlist (تمثيل بيانات جديد: -1 في availability = قائمة انتظار، بيع بحالة HL)، ودعم Direct Sell (بيع مباشر بصيغة أطول ومميزة من غير AN سابق). الجزء الفرعي "Open/Ghost/Arrival Unknown Segment" من نفس البند الأصلي اتصنّف بأمانة كـ⚠ استثناء مقصود (مش ❌) لأنه مفهوميًا محتاج أكتر من Segment، وده برّه نطاق قيد "Segment واحد فقط" الحالي.
النتيجة النهائية: 16 من 16 بند اتقفلوا. 14 بأوامر/سلوك جديد فعلي، و2 (جزء من بند واحد) اتصنّفوا ⚠ بدل ما يتسدّوا بأمانة.
اختبار الانحدار التراكمي النهائي عبر الدفعات السبعة: 87 حالة، 87 نجحت، 0 فشل.
ملحوظة مسجّلة (مراجعة مستقبلية): لو اتوفرت لاحقًا مواد رسمية من كورس EgyptAir Training Academy، لازم تتعمل مراجعة كاملة تانية على المرجع الرسمي ده بدل المرجع التقريبي المستخدم في المرحلة دي.
ملفات جديدة: certification-alignment-report.md, js/seatmaps.js, data/seatmaps.json.
ملفات معدّلة فعليًا: js/parser.js, js/pnr.js, js/pricing.js, js/queues.js — كل التعديلات إضافات موثّقة عبر 7 دفعات مُختبرة (تفصيل كل دفعة في certification-alignment-report.md قسم 3)، مفيش أي دالة handle قديمة اتلغى منطقها الأصلي — حتى handleSS (الدفعة 7) فضل مساره القديم زي ما هو 100%، والإضافات فروع جديدة بس.
حدود التقرير: js/errors.js وdata/errors.json متراجعوش وقت التنفيذ — جرد "فئات الخطأ" مبني على رسائل الخطأ الفعلية من parser.js/pnr.js مباشرة، مش على تصنيف errors.js الداخلي بالضبط.
المرحلة 12 — الاختبار النهائي ✅ (تم)
هدف المرحلة: محاكاة وردية شغل كاملة + Free Mode (تقييم نهائي بدون أي مساعدة).
مدخل من المرحلة 11: كل الـ16 فجوة كانت مقفولة فعليًا قبل بداية المرحلة دي — مفيش أي عنصر معلّق منها دخل نطاق المرحلة 12.
قرارات النطاق (اتاخدت بتفويض من Malik بمعايير افتراضية معقولة، بدل ما تتسيب مفتوحة):
Free Mode = أمر FREEMODEON/FREEMODEOFF جديد في coaching.js — بيقفل كل مساعدة خالص (التلميحات العادية + رسالة الوقفة الطويلة) لحد ما يتقفل. تقدم السيناريو (currentStepIndex) لسه بيتحسب عادي عشان النظام يعرف السيناريو خلص إمتى، بس من غير أي عرض على الشاشة.
محاكاة الوردية = سيناريو واحد مركّب جديد (id: 10) في data/scenarios.json، مش تجميعة من السيناريوهات القديمة — بيحاكي 5 مهام/عملاء ورا بعض من غير وقفة في 37 خطوة متصلة.
تغطية أوامر المرحلة 11: اتغطت فعليًا (مش اختياري فضل معلّق) — السيناريو الجديد بيمر على 24 أمر مختلف، فيهم كل حاجة تقريبًا من المرحلة 11: RT, XE, IG, ET, TTP, SM/ST, RM, QE/QT/QC/QD.
معيار "القفل": الوردية تتشغل من الأول للآخر من غير أي كراش أو رسالة فشل غير متوقعة + اختبار انحدار شامل (سيناريوهات قديمة + Free Mode toggle).
إحصائيات Free Mode: بتتسجل عبر localStorage بس في تتبع منفصل تمامًا عن تمرين الكوتشينج العادي (freeModeScenarioHistory / freeModeSessionsCompleted في performance.js) — عشان أداء "بدون مساعدة" ميتلخبطش مع تمرين فيه تلميحات.
باج حقيقي اتكشف واتصلح أثناء تنفيذ المرحلة 12
extractCode() في coaching.js (المسؤولة عن مطابقة كود الأمر اللي المتدرب كتبه بالخطوة المتوقعة في السيناريو) كانت بتتعرف على FQD/FXP بس كأكواد تلاتة حروف. أي كود تلاتة حروف تاني اتضاف في المرحلة 11 (TTP, FXB, DAC, DNA, FQN, FQR) كان بيتقطع غلط لأول حرفين (TTP → TT) وبيتقارن بالكود الكامل المتوقع فمتطابقش أبدًا — يعني أي سيناريو موجّه فيه خطوة TTP (أو أي كود تلاتة حروف تاني من المرحلة 11) كان هيفضل يعتبرها غلط للأبد حتى لو الأمر نجح فعليًا في parser.js. الباج ده اتأكد فعليًا بمحاكاة Node منفصلة قبل الإصلاح (نجاح parser + فشل تقدم السيناريو في نفس اللحظة)، واتصلح بقايمة صريحة لكل الأكواد التلاتة حروف المعروفة، واتأكد الإصلاح بنفس المحاكاة بعد كده.
فجوة تانية مكتشفة — مش اتصلحت، محتاجة قرار منفصل
js/performance.js (المرحلة 10 — قياس الأداء والتكيف) مش متصل فعليًا بأي حاجة في الكود الحالي. لا coaching.js ولا main.js بينادوا recordStepResult()/getAdaptiveTier()/onScenarioFinished()/isCategoryWeak() — غير initPerformance() بس وقت تحميل الصفحة (تحميل الإحصائيات المحفوظة، من غير أي استخدام فعلي بعد كده). يعني تكيف كثافة التلميحة المحددة حسب مستوى الأداء (المذكور كمكتمل في سجل المرحلة 10 فوق) مش شغال فعليًا في الكود الحقيقي، رغم إنه اتوصف كمكتمل وقتها. الفجوة دي اتسجلت هنا بدل ما تتصلح ضمن المرحلة 12 من غير الرجوع لـMalik — تصليحها معناه إعادة فتح سلوك مرحلة "مقفولة" (9/10)، وده قرار خارج نطاق المرحلة 12 المتفق عليه ومحتاج نقاش منفصل.
الاختبار
سيناريو 10 (37 خطوة) اتشغّل كامل بمحاكاة Node حقيقية (مش افتراضية) — عبر parser.js/pnr.js/pricing.js/ancillary.js/queues.js/seatmaps.js الفعليين، مع بيانات JSON اختبارية مبنية على أرقام رحلات حقيقية مؤكدة من certification-alignment-report.md (MS959 0820، MS971 1430). النتيجة: صفر أخطاء غير متوقعة عبر الـ37 خطوة، والسيناريو اتقفل بنجاح في آخر خطوة.
اختبار انحدار على سيناريو 1 القديم بالكوتشينج العادي (COACHINGON): تلميحة عامة تظهر صح على أمر غلط، تقدم صامت على أمر صح، رسالة اكتمال صح.
اختبار Free Mode toggle: FREEMODEON بيقفل التلميحات تمامًا على أمر غلط، FREEMODEOFF بيرجّعها صح على نفس نوع الغلط بعد كده.
⚠ حدود الاختبار: البيانات المستخدمة في المحاكاة (مطارات/رحلات/فنادق/سيارات...) بيانات اختبارية مبنية على البنية الحقيقية للكود مش نسخة كاملة من ملفات JSON الفعلية بتاعة Malik (متاحتش وقت المحاكاة) — فيه احتمال ضعيف إن تفاصيل بيانات حقيقية (زي حقل اسم مطار في airports.json لأمر DAC) تختلف، وده مسجّل صراحة في تعليق handleDAC من المرحلة 11 أصلًا.
ملفات معدّلة: js/coaching.js (إصلاح الباج + Free Mode + تلميحات لأكواد المرحلة 11)، js/performance.js (إضافة recordFreeModeScenario بس — بدون تفعيل أي جزء من نظام المرحلة 10 غير المتصل)، data/scenarios.json (إضافة سيناريو 10).
ملفات جديدة: مفيش.
6. مبادئ عمل ثابتة
الدقة قبل السرعة: أي مرحلة ما تتقفلش إلا لما تشتغل صح فعلًا، مش "شكلها شغالة".
الواقعية أولوية: أي اختيار بين "أسهل في البناء" و"أقرب للنظام الحقيقي" — نختار الأقرب للحقيقي.
كل حاجة Client-Side: ولا خطوة تتطلب سيرفر أو حساب خارجي غير المذكور فوق.
خطوة بخطوة على الموبايل: أي تعليمات بناء لازم تكون قابلة للتنفيذ من موبايل بدون خبرة برمجة.
الملف ده هو المرجع: أي تعديل في الهدف أو حزمة التقنيات أو خطة المراحل، لازم يتسجل هنا أول ما يتفق عليه، مش يفضل في ذاكرة المحادثة بس.
7. روابط مهمة
المستودع والموقع المباشر: amadeus-simulator
آخر تحديث: المرحلة 12 — الاختبار النهائي — مقفولة بالكامل، والمشروع كله مقفول (12 من 12 مرحلة). Free Mode (FREEMODEON/FREEMODEOFF) وسيناريو الوردية الكاملة (10، 37 خطوة) اتنفذوا واتأكدوا بمحاكاة Node حقيقية (صفر أخطاء). باج حقيقي اتكشف واتصلح أثناء التنفيذ (extractCode كانت بتقطع أكواد المرحلة 11 التلاتة-حروف غلط). فجوة تانية اتكشفت ومسجّلة بس مش اتصلحت (نظام تكيف الأداء من المرحلة 10 مش متصل فعليًا بأي حاجة — محتاجة قرار منفصل). تفاصيل كاملة في قسم المرحلة 12 فوق.
